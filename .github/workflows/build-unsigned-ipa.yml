name: Build Unsigned IPA (iOS 18.1)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build and Package
    runs-on: macos-15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode 16.1
        run: |
          sudo xcode-select -s /Applications/Xcode_16.1.app/Contents/Developer || echo "Xcode 16.1 not found, using default"
          xcodebuild -version

      - name: Build Unsigned App
        # 同时指定 -sdk iphoneos 和 -destination，避免触发平台未安装的报错
        run: |
          cd Mobile-O-App/app
          xcodebuild clean build \
            -project MobileO.xcodeproj \
            -scheme MobileO \
            -configuration Release \
            -sdk iphoneos \
            -destination "generic/platform=iOS" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED="NO" \
            CODE_SIGNING_ALLOWED="NO" \
            IPHONEOS_DEPLOYMENT_TARGET="18.1" \
            CONFIGURATION_BUILD_DIR="$(pwd)/build"

      - name: Package into Unsigned IPA
        run: |
          cd Mobile-O-App/app
          mkdir -p Payload
          mv build/MobileO.app Payload/
          zip -qr MobileO-Unsigned-iOS18.1.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MobileO-Unsigned-IPA
          path: Mobile-O-App/app/MobileO-Unsigned-iOS18.1.ipa
          retention-days: 7
